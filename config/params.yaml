main:
  uav_name_list: [uav2]
  offset: [0.0, 0.0, 0.0, 0.0]      # offset for all trajectories [x,y,z,yaw]
  delay: [0.0]            # delays before calling the service (in seconds)

timeout_for_calling_services: 3     # maximal waiting time for response after calling service

uav1:
  filename: "/data/eight_traj_offset.csv"
uav2:
  filename: "/data/eight_traj.csv"

loop: true
